export default function TopBar() {
    return (
        <div id="topBar">
            <input id="searchBar" placeholder="city name" />
            <h1 id="logo">Weather View</h1>
        </div>
    )
}